version: '2.2'

services:

  config:
    build: config

  registry:
    build: registry

  gateway:
    build: gateway

  uaa:
    build: uaa

  profile:
    build: profile

  media:
    build: media

  chat:
   build: chat

  comment:
    build: comment

  storage:
    build: storage

  web-client:
    build: web-client

  blog:
    build: blog

  statistics:
    build: statistics
