version: '2.2'

services:

  registry:
    build: registry

  gateway:
    build: gateway

  uaa:
    build: uaa

  profile:
    build: profile

  web-client:
    build: web-client

  blog:
    build: blog

  media:
    build: media

  statistics:
    build: statistics
