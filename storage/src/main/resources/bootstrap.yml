spring:
  application:
    name: storage
  cloud:
    config:
      uri: ${CONFIG_SERVICE_URI:http://config:8888}
      fail-fast: true
      username: ${CONFIG_SERVICE_USER}
      password: ${CONFIG_SERVICE_PASSWORD}
